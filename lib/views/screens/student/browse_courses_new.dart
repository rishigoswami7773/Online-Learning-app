import 'package:flutter/material.dart';

import 'browse_courses.dart';

class StudentBrowseCoursesScreen extends StatelessWidget {
  const StudentBrowseCoursesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const StudentBrowseCourses();
  }
}
